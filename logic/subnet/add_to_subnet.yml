---
- name: Ansible add vm to subnet
  hosts: "{{ hypervisor }}"
  user: root
  become: yes
    
  tasks:
    - name: Attach VM to subnet
      command: "virsh attach-interface --domain {{ vm_name }} --type network {{ subnet_network }} --model virtio"
      become: yes
